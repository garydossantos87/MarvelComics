import Foundation

extension Character {
    enum Detail {
        enum Constants {
            enum Header {
                static let heightPercentage: CGFloat = 0.35
                static let bottomPadding: CGFloat = 20
            }
        }
    }
}
