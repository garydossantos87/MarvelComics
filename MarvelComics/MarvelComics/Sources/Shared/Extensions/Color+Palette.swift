import SwiftUI

extension Color {
    static let primaryColor: Color = Color(UIColor(named: "PrimaryColor") ?? .primary)
    static let secondaryColor: Color = Color(UIColor(named: "SecondaryColor") ?? .yellow)
    static let tertiaryColor: Color = Color(UIColor(named: "TertiaryColor") ?? .lightGray) 
}
